# 数学绘图助手：联网确认与外部验证台账

最后核实：2026-07-31
适用 PRD：`数学绘图助手 PRD.md` v0.3  
用途：集中记录课程标准、平台、供应商、依赖、法律、实机和原型验证事项，避免把易过期结论散落在 PRD 中。

状态说明：

* **资料已确认**：已找到当前官方或一手资料；仍可能需要产品决策或原型验证；
* **待原型**：仅凭文档不能证明本项目中的实际行为；
* **待实机**：需要目标 Windows 设备或教学软件验证；
* **待供应商**：公开资料不足，需要 SimpleTex 等供应商书面确认；
* **待法务**：只能记录风险和依据，不能以产品或开发判断代替法律审查；
* **待产品决策**：事实已知，但范围、优先级或降级方式尚需负责人决定。

---

## 1. 正式决策引用（非决策台账）

本文件不签发或维护独立决策编号。正式状态、理由、替代方案和后果只在 `docs/decisions.md` 维护；本节仅把外部证据/待办映射到已有正式决策，避免形成第二套编号体系。

| 正式决策 | 本台账证据或关闭项 | 关系 |
|---|---|---|
| D-003 唯一常驻 RenderActor | C-09、P0-04 | Matplotlib 线程事实与本项目原型用于验证既定并发边界。 |
| D-005 自有受限 AST | C-08、P0-03 | 实验性解析事实支持完整消费与验证前复杂度门禁。 |
| D-006 一般非退化直线加入 M1.5 | C-01、C-02、P0-05、P0-07 | 课程/教材证据和范围签署用于确认产品范围。 |
| D-007 参数化圆锥曲线采样 | C-09、P0-06 | 库事实不能替代参数区间、残差、裁切和资源原型。 |
| D-012 QSettings 不存真实密钥 | C-07、C-11、P0-08～P0-11 | 供应商、隐私和客户端鉴权结论决定真实 OCR 是否可启用。 |
| D-014 Windows 11 主支持、Windows 10 条件兼容 | C-03、P0-01、P1-06 | 平台生命周期、依赖组合和实机矩阵共同约束最终支持声明。 |

---

## 2. 资料已确认的事实

### C-01 现行高中课程范围包含圆锥曲线方程

优先级：P0  
阻塞：M1.5 范围冻结  
状态：资料已确认

截至本次核实，教育部公开的现行普通高中数学课程依据仍是《普通高中数学课程标准（2017 年版 2020 年修订）》。其中“圆锥曲线与方程”明确包含：

* 椭圆的定义、标准方程和简单几何性质；
* 抛物线、双曲线的定义、几何图形、标准方程和简单几何性质；
* 椭圆、抛物线的简单应用；
* 通过曲线与方程进一步体会数形结合。

该内容位于选择性必修的平面解析几何主题。若产品面向参加高考的高中用户，圆锥曲线不应放到遥远的“可能扩展”中。

圆在课标中以“圆与方程”单列，要求标准方程、一般方程及基本位置关系；产品应支持圆，但 UI 术语宜与课标一致。

来源：

* [教育部：关于印发普通高中课程方案和课程标准（2017 年版 2020 年修订）的通知](https://www.moe.gov.cn/srcsite/A26/s8001/202006/t20200603_462199.html)
* [人民教育出版社：普通高中数学课程标准（2017 年版 2020 年修订）PDF](https://www.pep.com.cn/xw/zt/rjwy/gzkb2020/202205/P020220517519489596282.pdf)
* [人民教育出版社：选择性必修第一册圆锥曲线互动资源](https://www.pep.com.cn/xw/zt/xkzt/gsazy/gsaxbx1/)

剩余风险：教育部曾公开提及继续修订完善高中课程方案和课程标准；1.0 发布候选前应再次检查是否已有新版数学课标正式发布。

### C-02 多套教材并存，但核心圆锥曲线范围一致

优先级：P1  
阻塞：教材术语和示例库冻结  
状态：资料已确认，待产品决策

经审定的高中数学教材存在人教 A/B、北师大、苏教等多个版本。章节编号和内容组织不同，但椭圆、双曲线、抛物线的定义、标准方程和简单性质是共同核心。产品不应把界面术语硬绑定到某一教材章节号。

来源：

* [教育部：中小学教材选用管理办法](https://www.moe.gov.cn/srcsite/A26/moe_714/202303/t20230320_1437577.html)
* [教育部：2022 年中小学教学用书目录通知](https://www.moe.gov.cn/srcsite/A26/s8001/202204/t20220425_621597.html)
* [福建省教育厅：普通高中国家课程教学用书目录 PDF](https://jyt.fujian.gov.cn/xxgk/zfxxgkzl/zfxxgkml/zcwj/202205/P020220517540075245309.pdf)

### C-03 Windows 10 已结束常规支持，但 Qt 6.11 仍有兼容范围

优先级：P0  
阻塞：M0 依赖基线、M6 发布声明  
状态：资料已确认，待实机与产品决策

Microsoft 已于 2025-10-14 结束 Windows 10 常规支持。系统仍可运行，但不再默认获得常规技术支持和安全更新。另一方面，Qt 6.11 的官方平台表仍列出 Windows 10 1809+ x86_64 和 Windows 11，且注明 Qt 6.12 将是最后支持 Windows 10 的 Qt 版本。

因此必须区分：

* “Qt 技术上仍支持某 Windows 10 配置”；
* “Microsoft 仍对该操作系统提供常规安全支持”。

PRD 暂定 Windows 11 为主要支持，Windows 10 作为课堂存量设备兼容目标，不能把 Windows 10 写成与 Windows 11 同等级、无风险的长期平台。

来源：

* [Microsoft：Windows 10 support has ended](https://support.microsoft.com/en-US/Windows/Deployment/Updates-Lifecycle/windows-10-support-has-ended-on-october-14-2025)
* [Qt 6.11：Supported Platforms](https://doc.qt.io/qt-6/supported-platforms.html)

审计补充（2026-07-19）：当前开发机经 Microsoft OS Build 对照确认为 Windows 11 25H2（Build 26200.8655），而不是 Windows 10。用户确认该机此前已完成最小 `y=x^2` 绘图冒烟，并提供既有 `deployment/main.dist/main.exe` 打包目录截图；阶段 0A 还完成了 `main`、`main_window`、`plot_engine` 的非交互导入检查。上述证据仅说明当前 Windows 11 开发机上的早期演示可最小运行/已有打包产物，不构成 Windows 10 实机验证、完整 GUI 冒烟、正式发布验证或 P0-01 关闭依据。

### C-04 当前 Qt for Python 的 Python 与线程基础要求

优先级：P0  
阻塞：M0  
状态：资料已确认，待锁定版本和原型

当前 Qt for Python 入门文档要求官方 Python 3.10+。项目本地 `pyproject.toml` 目前要求 Python `>=3.12`，并使用 PySide6 `>=6.11.1`；发布前仍需把开放下限改成经过测试的可复现版本组合，而不是无限制追随新版本。

Qt 官方推荐的 worker-object 模式包括：创建 `QObject` Worker、用 `moveToThread()` 移动、通过 queued signal/slot 通信、在线程结束时 `deleteLater()`，关闭时 `quit()`/`wait()`。`QThread` 对象本身仍生活在创建它的线程；强制 `terminate()` 被明确视为危险做法。

来源：

* [Qt for Python：Getting Started](https://doc.qt.io/qtforpython-6/gettingstarted.html)
* [Qt for Python：QThread](https://doc.qt.io/qtforpython-6/PySide6/QtCore/QThread.html)
* [Qt for Python：Threads and QObjects](https://doc.qt.io/qtforpython-6/overviews/qtdoc-threads-qobject.html)

### C-05 Qt 6 的高 DPI 基线

优先级：P1  
阻塞：M5  
状态：资料已确认，待实机

Qt 6 在 Windows 上默认使用 Per-Monitor DPI Aware V2，并以设备无关像素处理大多数 Widget 几何；QImage、QPixmap 等像素缓冲仍需要正确处理 device pixel ratio。官方文档同时指出，非整数缩放可能暴露视觉问题，因此 125%、150%、175% 等比例必须实测。

来源：

* [Qt for Python：High DPI](https://doc.qt.io/qtforpython-6/overviews/qtdoc-highdpi.html)

### C-06 Windows 截图协议已变化

优先级：P0  
阻塞：M4  
状态：资料已确认，待打包与原型决策

Microsoft 当前文档使用新的 `ms-screenclip` 截图协议，并说明：通过 `redirect-uri` 安全接收结果需要具有应用身份的已打包 Windows 应用；未打包 Win32 调用者不能通过该回调取得结果。旧的截图启动文档已标记为弃用。

这意味着原 PRD 的“调用系统截图工具 → 结果一定进入剪贴板 → 监听一次变化”不能作为稳定契约。M4 必须在以下方案间用原型选择：已打包回调、用户主动粘贴，或经证明可靠的短时剪贴板观察。

来源：

* [Microsoft Learn：Launch Snipping Tool](https://learn.microsoft.com/en-us/windows/apps/develop/launch/launch-snipping-tool)
* [Microsoft Learn：Launch screen snipping（Deprecated）](https://learn.microsoft.com/en-us/windows/apps/develop/launch/launch-screen-snipping)

### C-07 SimpleTex 当前 API 与鉴权信息

优先级：P0  
阻塞：M3  
状态：接口资料已确认；隐私、条款和客户端授权仍待供应商

当前官方文档列出了公式识别轻量与标准接口，使用 `POST`、`multipart/form-data` 和 UAT 或 APP 鉴权；单图返回示例目前包含 LaTeX、置信度和供应商 request ID。产品模型可以读取置信度，但不应把它设成永久必需字段。

鉴权文档还明确提醒：如果应用部署在用户端，不应把核心鉴权接口或 APP Secret 暴露在客户端，应考虑临时应用授权令牌。当前核心 MVP 又不含自有服务端，因此发布方案必须在“用户个人 UAT + Windows 凭据存储”“供应商正式临时令牌”或“关闭真实 OCR”中作出明确选择。

来源：

* [SimpleTex：公式识别 API](https://doc.simpletex.cn/zh/api/api_formula_recognition.html)
* [SimpleTex：鉴权说明](https://doc.simpletex.cn/zh/api/auth_note.html)
* [SimpleTex：开放平台说明](https://doc.simpletex.cn/zh/api/api_note.html)

注意：价格、免费额度、QPS、并发、模型版本和响应字段都是易变信息，M3 开始和 1.0 发布候选前必须再次核实，不能写死为长期产品承诺。

### C-08 SymPy LaTeX 解析仍是实验性能力

优先级：P0  
阻塞：M1 解析策略、M3 OCR 兼容  
状态：资料已确认，待安全原型

SymPy 当前文档把 LaTeX 解析实现标为实验性，并同时提供 ANTLR 与 Lark 后端。文档还提示 ANTLR 后端可能只解析合法前缀而未报错。这支持 PRD 的两个约束：OCR LaTeX 不能直接视为可信表达式；解析器必须验证完整消费并在 AST 层执行白名单和复杂度检查。

来源：

* [SymPy：Parsing / Experimental LaTeX Parsing](https://docs.sympy.org/latest/modules/parsing.html)

### C-09 Matplotlib 线程与非交互式渲染边界

优先级：P0  
阻塞：M1 RenderActor、M1.5  
状态：资料已确认；M1 RenderActor/Agg 原型曾于阶段 10 登记关闭，最终只读验收发现两个阻塞后暂时重开；最小修复已通过后续架构核对和最终独立技术复验，阶段 10 通过并重新关闭 P0-04；阶段 11 UI 整合尚未验收，M1.5 曲线原型仍待后续阶段

Matplotlib 官方说明其整体不是线程安全的；多线程使用时应由应用串行化 Artist 访问，并使用非交互式后端。这支持项目采用唯一 RenderActor、Agg、Figure/Canvas 局部所有权和有序资源释放。

`contour` 能绘制二维网格的零等值线是库能力事实，但“能绘制”不等于它是本项目已分类直线/圆锥曲线的最佳主方案。当前架构决策改用几何参数化采样；contour 只保留为诊断对照或未来任意隐式曲线的独立候选，不据此扩张 PRD 范围。

来源：

* [Matplotlib FAQ：Work with threads](https://matplotlib.org/stable/users/faq.html#work-with-threads)
* [Matplotlib：contour API（能力事实与诊断对照）](https://matplotlib.org/stable/api/_as_gen/matplotlib.pyplot.contour.html)

阶段 10D 已在锁定版本下确认 FigureCanvasAgg 的 formal Engine → sampler → Agg 真实链路、唯一常驻单线程消费者、latest-wins、协作取消、有界关闭和 Figure/Canvas/BytesIO/数组释放，并已通过阶段 10 最终技术复验。该结论不构成阶段 11 的 UI、MainWindow、preview、clipboard、bootstrap 或 M1 端到端整合验收。仍需后续原型确认：参数化直线/圆锥曲线的可见参数区间、分支、裁切、性能、内存与残差容差；这些 M1.5 问题不能用阶段 10 的显函数结果外推。

### C-10 打包工具当前关系

优先级：P1  
阻塞：M6  
状态：资料已确认，待构建对比

当前 `pyside6-deploy` 是 Qt for Python 官方部署工具，并以 Nuitka 为底层包装；它支持 onefile 和 standalone 等模式。是否选它仍需与项目实际资源、启动速度、杀毒误报、调试能力和 Windows 截图打包身份需求一起验证。

来源：

* [Qt for Python：pyside6-deploy](https://doc.qt.io/qtforpython-6/deployment/deployment-pyside6-deploy.html)
* [Qt for Python：Deployment](https://doc.qt.io/qtforpython-6/deployment/index.html)

### C-11 未成年人和课堂截图具有较高隐私风险

优先级：P0  
阻塞：M3 真实 OCR、M6 发布  
状态：法律事实已确认，具体方案待法务

《中华人民共和国个人信息保护法》将不满十四周岁未成年人的个人信息列为敏感个人信息，并对处理目的、必要性、严格保护措施、单独同意、监护人同意和专门规则提出要求。课堂截图可能同时包含姓名、成绩、头像或其他学生信息，不能仅靠一句“请谨慎上传”完成合规设计。

本文件不提供法律意见。M3 前应由有权限的人员评审：产品在各使用场景中的角色、告知和同意方式、供应商关系、处理地点、保存期限、删除机制、影响评估、未成年人规则及可能的数据出境问题。

来源：

* [中国人大网：中华人民共和国个人信息保护法](https://www.npc.gov.cn/npc/c2/c30834/202108/t20210820_313088.html)
* [国家网信办：网络数据安全管理条例](https://www.cac.gov.cn/2024-09/30/c_1729384452307680.htm)

### C-12 PySide6 许可不是“无需处理”

优先级：P0  
阻塞：M6  
状态：许可类型已确认，具体义务待审核

Qt for Python 官方页面列出 PySide6 可在 LGPLv3/GPLv3 或商业许可下使用。最终发布方式、动态链接、许可证文本、用户替换库的权利、Qt 插件、第三方字体和其他 Python 包的义务必须形成完整清单；不能只在发布包里随意放一份许可证文件。

来源：

* [Qt for Python 官方主页与许可说明](https://doc.qt.io/qtforpython-6/)

---

## 3. 待确认清单

### P0：在指定关闭阶段验收前关闭或形成批准的降级决定

关闭锚点按开发步骤清单的阶段编号解释，不等同于“里程碑开始前”。到达锚点仍未关闭时，不得完成对应阶段/里程碑验收；只有项目所有者明确批准、且已同步 PRD/decisions/architecture/清单影响的降级决定可以替代关闭。

- [x] **P0-01 / M0 / 关闭锚点：阶段 0 验收。锁定可复现依赖基线。** 已关闭。阶段 0 的精确 Python 与核心依赖基线已经固定并同步；当前批准组合已完成 Windows 11 standalone 最小实机冒烟，并由同一 standalone 候选在至少一台 Windows 10 存量设备完成启动、`y=x^2` 绘图和正常关闭的跨机器最小冒烟。该关闭不构成正式打包工具、发布格式或 Windows 10 正式兼容矩阵结论。

  历史审计记录（2026-07-19）：当时仅有早期演示/既有打包产物证据，P0-01 保持未关闭。正式关闭登记见“P0-01 正式关闭登记（2026-07-20）”；阶段 0 仍未完成，P0-02 仍未关闭。
- [x] **P0-02 / M0 / 关闭锚点：阶段 0 验收。确定课堂基准设备。** **已关闭（经项目所有者批准延期实机验证）。** 当前高配置 Windows 11 电脑仅为开发机和开发参考环境，不代表学校电脑、教学一体机或代表性低配置设备。当前尚无真实学校电脑、教学一体机或经确认的代表性低配置基准设备；该设备上的性能、DPI、触控和多显示器观察不得外推为课堂结论。项目所有者批准在阶段 0 采用延期实机验证的降级关闭；在完成后续实机验证前，不得声称软件已满足课堂设备兼容性或性能要求。

  尚缺代表设备 CPU、内存、磁盘，Windows 版本与 Build，分辨率和 DPI，触控能力，多显示器环境，是否为教学一体机，以及课堂场景性能数据。阶段 12、15 等较早性能基准如只能在当前开发机执行，只能标记为“开发参考数据”，不能作为课堂性能达标依据。

  **阶段 26 开始时，重新打开 P0-02 的课堂设备实机验证风险。** 阶段 26 应在真实学校设备，或项目所有者批准的代表性低配置 Windows 10/11 设备上重新验证触控、100%–200% DPI、多显示器、界面可用性以及典型启动和绘图性能。上述风险最迟必须在阶段 30 发布候选验收前完成实机验证或形成新的明确发布降级决定；否则不得宣称课堂设备兼容或形成公开发布候选。阶段 26 和阶段 30 已分别承担课堂 DPI、触控、多显示器及最终实机发布验收，本延期事项不得永久搁置。
- [x] **P0-03 / M1 / 关闭锚点：阶段 7 验收。验证安全解析方案。已关闭（本次阶段 7 最终裁定）。** 对手动文本、隐式乘法、Unicode、`|x|`、对数语义、尾部非法文本和恶意输入做最小原型；禁止只证明“SymPy 能解析”。安全解析原型证据成立；本项关闭不表示 M1 已完成，后续阶段仍须分别实施和验收。
- [x] **P0-04 / M1 / 关闭锚点：阶段 10 验收。验证 RenderActor 与 Matplotlib 边界。已关闭（阶段 10 最终技术复验，2026-07-31）。** 关闭范围为固定 Matplotlib Agg、唯一常驻单线程 RenderActor、一个 current 加一个 latest-wins pending、协作取消、有界 shutdown 与资源释放。首次关闭、两个阻塞和暂时重开历史保留在第 7 节，最终独立技术复验与重新关闭证据见第 8 节。本关闭不构成阶段 11 UI、bootstrap、MainWindow、preview、clipboard 或 M1 端到端整合验收。
- [ ] **P0-05 / M1.5 / 关闭锚点：阶段 13 开始前。最终签署直线与圆锥曲线首期范围。** 当前 PRD 纳入数值系数非退化一般直线，以及轴向平行、可平移、数值系数的非退化二次曲线；产品负责人确认不扩张到旋转/任意一般二次曲线、方程组或自动求交，并确认把教学标注推迟到 M5 候选。未签署时不开始 M1.5，并记录保留已验收 M1 的降级。
- [ ] **P0-06 / M1.5 / 关闭锚点：阶段 14 验收。完成参数化采样原型。** 验证直线与视口求交、圆/椭圆角参数、双曲线两支、抛物线开口方向、可见参数区间、裁切提示、残差容差、采样/分支/内存上限和性能目标；contour 仅作诊断对照。失败时停止 M1.5，不得把 contour 静默替换为正式主方案。
- [ ] **P0-07 / M1.5 / 关闭锚点：阶段 15 验收。用真实教材表达式验收。** 收集不含个人信息的椭圆、双曲线、抛物线、圆样例，覆盖标准式、平移式、Unicode、分数和左右交换。无法完成时不得宣称 M1.5 已完成教材验收；若要移出核心 MVP，必须新增范围降级决策。
- [ ] **P0-08 / M3 / 关闭锚点：阶段 21 验收。供应商书面确认数据处理。** SimpleTex 的图片和结果处理地点、保存期限、删除机制、日志/训练用途、子处理者、未成年人/教育场景条款、数据出境和安全事件流程。未关闭则阶段 22 不接真实服务，继续模拟 OCR。
- [ ] **P0-09 / M3 / 关闭锚点：阶段 21 验收。供应商接口与费用复核。** 固定接口、模型、鉴权、超时、最大文件/像素、响应结构、QPS、并发、429、配额、计费、重试是否重复计费以及服务可用性承诺。未关闭则阶段 22 不接真实服务，继续模拟 OCR。
- [ ] **P0-10 / M3 / 关闭锚点：阶段 21 验收。选择客户端鉴权方案。** 验证 Windows Credential Manager 接入、用户 UAT 生命周期或供应商临时令牌；不能满足时，1.0 关闭真实 OCR，仅保留本地图片核查和模拟流程。
- [ ] **P0-11 / M3 / 关闭锚点：阶段 21 验收。完成隐私与未成年人合规评审。** 明确告知、同意、裁剪/遮挡、最短保存、删除、撤回、诊断包和供应商关系。负责人不得只用开发测试结论代替法务意见；未关闭则阶段 22 不接真实服务。
- [ ] **P0-12 / M3 / 关闭锚点：阶段 23 验收。建立 OCR 兼容数据集。** 实测 SimpleTex 输出到规范化、分类、绘图的成功率，特别覆盖 `\frac`、上标、隐式乘法、左右式、多行噪声和四类圆/圆锥曲线。未达到经批准口径时关闭真实 OCR，保留模拟流程，不把未验证结果直接送入绘图。
- [ ] **P0-13 / M4 / 关闭锚点：阶段 24 验收。选择 Windows 截图策略。** 比较 MSIX/应用身份回调、用户主动粘贴和短时剪贴板观察；实测成功、取消、超时、相同截图、多显示器和外部程序同时改写剪贴板。若三种策略均不可接受，则以正式降级决定从 M4 移除系统截图，只保留本地图片导入。
- [ ] **P0-14 / M6 / 关闭锚点：阶段 30 验收。完成目标软件剪贴板矩阵。** 明确 PowerPoint、Word、WPS、希沃白板的具体版本与接受格式，验证透明/不透明背景、DPI、尺寸、粘贴清晰度和重复复制。失败的软件/版本必须从支持声明移除或以已验证替代项形成产品降级，不能继续无证据宣称兼容。
- [ ] **P0-15 / M6 / 关闭锚点：阶段 28 验收。完成许可清单。** PySide6/Qt、Matplotlib、NumPy、SymPy、ContourPy、Nuitka/PyInstaller、字体、SVG 图标及所有传递依赖逐项记录许可证与发布义务。无发布降级；未关闭只能保留开发构建。
- [ ] **P0-16 / M6 / 关闭锚点：阶段 30 验收。完成代码签名、SmartScreen 与杀毒验证。** 确定证书、签名流程、信誉建立、误报处置、自动扫描和发布包校验值。未关闭不得形成公开 1.0 发布候选；如批准，只能降级为有明确风险说明的内部测试构建。
- [ ] **P0-17 / M6 / 关闭锚点：阶段 30 验收。再次检查现行课程标准和供应商文档。** 记录发布日期、版本或页面更新时间，避免使用本文件 2026-07-19 的快照直接发布。无使用旧快照直接发布的安全降级。

### P1：重要但可在明确降级后延期

- [ ] **P1-01 / 选择或降级锚点：阶段 13 开始前。用户范围确认。** 核心用户是只完成高中学业要求，还是包含高考备考；后者决定圆锥曲线、直线与曲线位置关系和教学标注的优先级。未决定时记录“维持当前高中教师基线、不新增备考专属范围”的延期决定。
- [ ] **P1-02 / 选择或降级锚点：阶段 13 开始前。教材版本策略。** 是否全国中性术语，还是首发绑定某省、某教材版本；若全国通用，建立术语映射而不是章节号映射 UI。未决定时记录“采用全国中性术语、不绑定章节号”的阶段性策略，避免阻塞 P0-07 样例验收。
- [ ] **P1-03：课堂增强深度。** 决定 M5 是否加入焦点、准线、顶点、渐近线、离心率、参数联动，以及是否需要直线与圆锥曲线交点/相切判断。
- [ ] **P1-04：数学字体与中文字体。** 验证缺字、上标、负号、希腊字母、高 DPI 清晰度和字体打包许可。
- [ ] **P1-05：发布模式对比。** 比较 pyside6-deploy/Nuitka 的 onefile、standalone 与必要备选工具在启动速度、包体、资源完整性、调试、截图身份和杀毒误报上的表现。
- [ ] **P1-06：Windows 10 维护终点。** 结合学校设备调研决定支持期限、最低版本、ESU 要求、已知风险和未来 Qt 升级策略。
- [ ] **P1-07：日志与诊断体验。** 确定默认保留期限、容量、导出内容、脱敏测试和用户删除入口。

---

## 4. P0-01 正式关闭登记（2026-07-20）

```text
ID：P0-01
核实日期：2026-07-20
核实人：既有实施记录未单列人员姓名；本登记不虚构姓名
批准人/责任角色：项目所有者 / 总架构师（既定裁定）
状态：已关闭
阻塞里程碑：阶段 0 验收
官方来源及版本：既有 0B-1 依赖审计与当前锁定版本记录
原型或实机环境：Windows 11 x64 + MSVC cl 14.5；至少一台 Windows 10 存量设备
观察结果：依赖同步、导入、standalone 构建及两台机器最小冒烟通过
产品决策：P0-01 技术关闭条件满足
降级方案：无
剩余风险：Windows 10 正式矩阵、中文字体、正式打包和发布事项仍待后续阶段
已同步文件：联网确认.md；docs/audits/stage-0B-2-controlled-dependency-baseline-and-smoke.md
下次复查日期：阶段 28–30 或发布候选前
```

关闭说明：阶段 0 的精确 Python 与核心依赖基线已经固定并同步；当前批准组合已完成 Windows 11 standalone 最小实机冒烟，并由同一 standalone 候选在至少一台 Windows 10 存量设备完成启动、`y=x^2` 绘图和正常关闭的跨机器最小冒烟。该关闭不构成正式打包工具、发布格式或 Windows 10 正式兼容矩阵结论。

P0-02 仍未关闭，阶段 0 仍未完成；不得据此开始阶段 1。

---

## 5. 单项核实记录模板

完成清单项时复制以下模板，并把结论同步到 `docs/decisions.md`：

```text
ID：
核实日期：
核实人：
批准人/责任角色：
状态：资料已确认 / 待原型 / 待实机 / 待供应商 / 待法务 / 已关闭
阻塞里程碑：
官方来源及版本：
原型或实机环境：
观察结果：
产品决策：
降级方案：
剩余风险：
已同步文件：
下次复查日期：
```

---

## 6. P0-03 阶段 7 正式关闭登记（2026-07-22）

本节登记当前受限手动显函数安全解析原型及阻塞反例定点修复的正式关闭证据。阶段 7 已通过，P0-03 的安全原型证据成立并正式关闭；本登记不将该结论扩大为 M1 或后续阶段已经完成。

```text
ID：P0-03
核实日期：2026-07-22
核实人：未单列人员姓名；本登记不虚构姓名
批准人/责任角色：项目所有者 / 总架构师（本次阶段 7 最终裁定）
执行环境：Windows 11 10.0.26200 AMD64；Python 3.12.11；uv 0.9.28；pytest 9.1.1
Git 基线：master / 4d0416b feat: add normalized input lexical frontend
状态：已关闭
正式裁定：阶段 7：通过；P0-03：安全原型证据成立，准予正式关闭
```

### 安全边界、AST 与依赖方向

* 安全边界是项目自有 `RestrictedExpression` 联合，不是 Python AST、SymPy 对象或无类型 dict；节点为 frozen+slots dataclass，参数 tuple 化，并保存 normalized/source spans。Model-owned 构造不变量现在检查数字 lexeme、变量/常量/函数封闭名称、运算符、arity 及全部子节点，tuple 内的 dict/list/object 或未知节点在构造边界拒绝。
* AST 位于 `math_drawing_assistant/models/restricted_ast.py`；Engine 导入 Models。精确搜索 `rg -n -g '*.py' 'math_drawing_assistant\.engine' math_drawing_assistant/models` 为 0 命中，不存在 `models → engine` 反向依赖。
* 原始字符串只进入既有 `normalize_input → tokenize → split_equation`，之后 parser 只消费 typed token。阶段 7 未导入 SymPy、NumPy 或 Matplotlib，未构造数值函数，也未进入采样/视口/渲染。
* AST 节点、深度、函数参数、字面量指数和有理数字面量预算均在 parser 构造期、任何数学执行之前检查；有理检查对分子和分母一致解包单层一元 `+/-`，不进行 Fraction、SymPy、化简或代数求值。项目未修改 Python 全局 recursion limit。

### 正式生产调用图与 validated-result 构造边界

当前正式生产调用图：

```text
analyze_explicit_function
→ normalize_input
→ tokenize
→ split_equation
→ parse_input
→ classify_plot
→ validate_explicit_candidate
→ 签发 parser/active limits 版本一致的内部 contract
→ model-owned `_create_validated_explicit_expression`
→ ValidatedExplicitExpression
```

独立 validator 仍只返回 Engine 内部 frozen `ExplicitValidation`。`ValidatedExplicitExpression` 普通公共构造明确拒绝任意 AST/版本字符串；完整入口验证成功后才调用未公开导出的 model-owned 工厂，工厂再次检查整棵 AST、自由变量、span 和 typed limits contract。该边界不依靠从 `models.__init__` 移除类型，也不宣称 Python 层具有密码学意义上的绝对私有性。源码搜索：

```powershell
rg -n -g '*.py' 'ValidatedExplicitExpression\(|_create_validated_explicit_expression\(|PlotSceneSpec\(' math_drawing_assistant
```

观察结果：正式 Engine 中直接 `ValidatedExplicitExpression(` 为 0 个构造点；`_create_validated_explicit_expression(` 的调用只有 `math_drawing_assistant/engine/validators.py` 中完整入口验证成功后的 1 处，定义位于 Models；正式 Engine 中 `PlotSceneSpec(` 为 0 个构造点。parser/classifier/validator 低层接口仍公开用于组合和测试，因此“统一入口”只描述当前正式调用图；安全保证来自构造不变量、validator 和受控工厂，不来自源码字符串扫描本身。

### Parser、classifier 与 validator 观察

* parser 完整消费；优先级、加减/乘除左结合、幂右结合、`-x^2`、`(-x)^2`、`x^-2`、`-x^-2`、`x^2^3` 均有节点级行为断言，不靠格式化字符串比较。
* 隐式乘法只允许 `NUMBER+VARIABLE`、`NUMBER+LEFT_PAREN`、`RIGHT_PAREN+LEFT_PAREN`；`x2`、`x(...)`、`)x`、`2sin(...)`、`2pi` 均拒绝。
* BAR 统一构造 `abs` FunctionCallNode；`|x|+|x+1|` 与 `abs(abs(x))` 成功，`||x||` 结构化拒绝。
* `ln(x)`、`lg(x)`、`log(x,2)`、`log(x,10)` 成功；裸 `log(x)`、0/1/负数/变量/复合底数均按稳定错误码拒绝。
* classifier 只交付 expression、`y=rhs`、`lhs=y` 三类显函数候选；`x=y` 直接互换成功。其余方程统一返回中性 `unsupported_equation`，不再用平方项猜测圆锥，也不把高次或含函数方程称为一般一次方程；阶段 7 不做移项、次数/系数提取、展开、化简或 solve。
* Validator 限制显函数自由变量为 `x` 或空集，防御性检查封闭节点、运算、函数、常量、arity、指数和 log 底数；validated-result 工厂再检查 AST 图、自由变量和 contract。普通公共构造不能凭任意字符串产生名义上的 validated result；伪造 parser limits 版本得到稳定 `invalid_ast`。
* 最终阻塞修复：`validate_explicit_candidate` 对封闭 AST 联合先递归执行精确类型检查，覆盖根节点、Unary/Binary 子节点和 Function arguments。携带 mutable `dict` payload 的 `NumberNode` 等节点子类会结构化拒绝为 `invalid_ast`；新增 11 项回归覆盖各个位置与全部六类节点子类。普通精确 AST 与完整 `analyze_explicit_function` 入口仍保持成功。

### SourceMap 与 EOF 定位结果

代表性通过结果：

```text
"x²"       → AST source span [0,2)
"x**2"     → AST source span [0,4)
"２（x＋１）" → AST source span [0,6)
"2(x+1)"   → AST source span [0,6)
"｜x｜"      → AST source span [0,3)
"sin(x"    → delimiter_mismatch at original EOF [5,5)
```

阶段 6 机械识别未闭合左括号/BAR 后，正式组合入口通过现有 SourceMap 确认开分隔符并映射到原文实际 EOF；尾随空格也计入原文 EOF。多余右括号仍指向该字符本身，不伪装成 EOF。

### 限制边界测试

* 阶段 6 数字总位数、小数位数、token、字符和括号嵌套继续覆盖上限内、恰好上限和超一项，直接读取 `DEFAULT_LIMITS`。
* 阶段 7 为隔离限制，使用 `dataclasses.replace(DEFAULT_LIMITS, ...)` 构造仅降低目标字段的不可变测试配置；AST 节点数、AST 深度、绝对指数和函数参数覆盖构造边界。有理数使用互不相同的 numerator/denominator 配置，对 `123/1`、`+123/1`、`-123/1`、`1/123`、`1/+123`、`1/-123` 分别覆盖 4（上限内）、3（恰好）、2（超一位），并断言错误码、中文消息、原文 span 和 recoverable。
* 超限在构造下一个 AST 节点、读取额外函数参数或转换有界整数字面量时失败；没有巨大整数幂计算或完整超限 AST 构造。

### 固定随机与恶意输入

```text
mutation 固定种子：20260721
mutation 生成条目数：90
mutation 唯一字符串数：31
mutation typed success：0
mutation 结构化拒绝：90

mixed fuzz 固定种子：20260722
mixed fuzz 生成条目数：600
mixed fuzz 唯一字符串数：556
mixed fuzz typed success：52
mixed fuzz 结构化拒绝：548
未处理异常：0
```

Mutation 规则覆盖删除闭合符号、多余右括号、第三个 log 参数、非法尾部、未知名称、白名单外邻接、超限嵌套、空/多余参数与嵌套 BAR。Mixed fuzz 使用合法种子加有界 ASCII/全角/Unicode 运算符、控制符、标识符与标点混合。相同输入重复结果稳定；每组结束后 `sin(x)` 仍能正常返回 typed success。两组存在重复字符串，只作为固定种子、有界的最小健壮性和确定性证据，不等同于 690 个独立案例，也不是形式化证明。

代表性恶意输入包括 `x.__class__`、`().__class__`、`__import__("os")`、`eval(x)`、`exec(x)`、`compile(x)`、`ast.parse(x)`、`ast.literal_eval(x)`、`open(x)`、`globals()`、`locals()`、`lambda`、字符串/字节串、属性、下标、列表、字典、集合、comprehension 和关键字参数；全部在阶段 6/7 正式入口结构化拒绝，无执行路径。

### 禁止 API 搜索与命中分类

搜索范围：正式 `math_drawing_assistant/engine/**/*.py`；模式为单词边界加调用括号。

```powershell
$keywords=@('eval','exec','compile','ast\.parse','ast\.literal_eval','sympify','parse_expr','parse_latex','solve','expand','simplify','factor','lambdify')
foreach($keyword in $keywords){ rg -n -g '*.py' -- ('\b'+$keyword+'\s*\(') math_drawing_assistant/engine }
```

各关键词正式 Engine 调用命中数：`eval 0`、`exec 0`、`compile 0`、`ast.parse 0`、`ast.literal_eval 0`、`sympify 0`、`parse_expr 0`、`parse_latex 0`、`solve 0`、`expand 0`、`simplify 0`、`factor 0`、`lambdify 0`。测试和文档中的恶意字符串不计为正式链路命中。

### 测试命令与观察结果

```powershell
$env:PYTHONPATH='.'
uv run --locked pytest -v tests/engine/test_parser.py tests/engine/test_plot_classifier.py tests/engine/test_validators.py tests/engine/test_stage7_safety.py
# 233 passed（最终子类绕过修复前的历史定向复跑）

uv run --locked pytest -v tests/engine/test_normalizer.py tests/engine/test_source_map.py tests/engine/test_tokenizer.py tests/engine/test_equation_splitter.py tests/engine/test_input_frontend_robustness.py
# 114 passed

uv run --locked pytest -q tests/test_errors.py tests/test_limits.py tests/engine/test_normalizer.py tests/engine/test_source_map.py tests/engine/test_tokenizer.py tests/engine/test_equation_splitter.py tests/engine/test_input_frontend_robustness.py tests/engine/test_parser.py tests/engine/test_plot_classifier.py tests/engine/test_validators.py tests/engine/test_stage7_safety.py
# 380 passed（最终子类绕过修复前的阶段 5 limits/error registry + 阶段 6/7 历史定向复跑）

uv run --locked pytest -v tests/engine
# 347 passed（最终子类绕过修复前的历史复跑）

uv run --locked pytest -v
# 483 passed（最终子类绕过修复前的历史复跑）

git diff --check
# exit 0
```

最终关闭复跑结果：新增的 11 项子类绕过回归均通过；`tests/engine` 为 **359 passed**，全量为 **495 passed**，`git diff --check` 通过。上述 233/380/347/483 为最终子类绕过修复前的明确历史复跑记录，不是当前最终计数。Pyright 仍受 D-009 `Proposed` 和项目所有者批准门约束，本轮未安装、配置或运行类型检查器。

### 观察结论与剩余风险

观察结论：本轮已针对 AST/validated-result 构造不变量、带符号有理限制、classifier 类别猜测及最终发现的 AST 节点子类绕过完成定点修复并新增反例测试。独立最终复核确认修复成立，正式裁定为“阶段 7：通过；P0-03：安全原型证据成立，准予正式关闭”。

剩余风险：受控工厂和下划线内部 contract 维护正常公共构造边界，不试图提供密码学意义的 Python 私有性；未来新增 AST 节点、函数或 limits 版本时必须同步 model-owned 不变量、validator 和反例测试。本证据只覆盖当前阶段 7 的受限手动显函数语法、当前白名单和当前 limits；不证明通用数学表达式、任意 SymPy 表达式、OCR LaTeX、阶段 8 数值转换与采样、阶段 10 RenderActor 或 Matplotlib、M1.5 一般直线、圆和圆锥曲线，或未来扩大函数或语法白名单后天然安全。阶段 8、9、10、11、12 仍未实施或验收。PRD 将 classifier 写成返回 typed PlotItemSpec，而 architecture/阶段清单采用 candidate → validator → validated result 的阶段性接口，仍是非阻塞文档歧义，本轮未修改 PRD。性能、真实课堂设备和发布边界仍由后续阶段与既有 P0 项裁定。

正式状态：已关闭。

---

## 7. P0-04 阶段 10 RenderActor/Agg 历史关闭、阻塞重开与最终复验前状态（2026-07-31）

本节登记阶段 10A/10B/10C 既有实现之上的 10D 独立证据。真实 executor 只存在于 `tests/workers/test_render_actor_agg_probe.py`，使用正式 `PlotSceneRequest` / `PlotItemRequest`，并只通过公开 API 组合：

```text
analyze_explicit_function
→ build_explicit_scene_spec
→ resolve_single_explicit_viewport
→ RenderPlanBuilder.build
→ sample_explicit_function(cancellation_probe=token)
→ render_explicit_png(cancellation_probe=token)
→ PlotSceneResult
```

成功路径使用正式 builder 签发的 approval receipt 和 sampler provenance，不伪造 seal，不用 `object.__setattr__`。任一 `ErrorInfo` 映射为保留输入 `request_id` / `scene_revision` 的失败 `PlotSceneResult`；PNG bytes 映射为成功结果；`SamplingCancelled` / `RenderCancelled` 只形成无新错误码的取消结果，并由 Actor token/result gate 抑制。

```text
ID：P0-04
核实日期：2026-07-31
核实人：本轮 Codex 桌面实施会话；未虚构自然人姓名
批准人/责任角色：项目所有者 / 总架构师（本轮目标授权）
执行环境：Windows 11 x64 build 10.0.26200；AMD64；Python 3.12.11；uv 0.9.28；pytest 9.1.1；PySide6 6.11.1；Matplotlib 3.11.1；NumPy 2.5.1
Git 基线：master / d8b023c88ee324f7f8c1cb2e36d24c84e25c8940（feat: add budgeted Agg renderer）
初始工作树：既有 10A–10C 未提交改动：math_drawing_assistant/app_controller.py、math_drawing_assistant/workers/、tests/test_app_controller.py、tests/workers/
历史状态：曾登记为已关闭
历史裁定：曾登记为“P0-04 具备关闭条件；阶段 10 严格通过”
最终复验前状态：等待阶段 10 最终独立复验
最终复验前裁定：最终只读验收发现两个阻塞，最小修复已经实施；P0-04 暂时保持打开
```

### 历史自动测试与独立 probe 统计（首次关闭登记）

所有命令均使用锁文件，并设置 `PYTHONDONTWRITEBYTECODE=1`、`PYTHONPATH=.` 和 `pytest -p no:cacheprovider`：

以下 21/21/9/55/317/840 为两个阻塞修复和最终独立技术复验前的历史统计，不是当前最终计数。

```powershell
uv run --locked pytest -q -p no:cacheprovider tests/workers/test_render_actor.py
# 21 passed

uv run --locked pytest -q -p no:cacheprovider tests/workers/test_render_actor_shutdown.py
# 21 passed

uv run --locked pytest -q -p no:cacheprovider tests/workers/test_render_actor_agg_probe.py
# 9 passed

uv run --locked pytest -q -p no:cacheprovider tests/workers
# 55 passed

uv run --locked pytest -q -p no:cacheprovider tests/engine/test_spec_builder.py tests/engine/test_viewport_resolver.py tests/engine/test_render_plan.py tests/engine/test_samplers.py tests/engine/test_renderer.py tests/workers tests/test_app_controller.py tests/test_models.py
# 317 passed

uv run --locked pytest -q -p no:cacheprovider
# 840 passed
```

静态检查结果：

* 活动产品包和入口链无 `QThread.terminate()`，RenderActor 使用有上限的 `wait(timeout_ms)`；
* formal renderer 不导入 `pyplot`；fresh process 中 Actor 导入不加载 Matplotlib，renderer 导入后 `get_backend(auto_select=False) is None`、`backend_agg` 已加载、`backend_qtagg` 与 `matplotlib.pyplot` 未加载，interactive 为 false；PySide6 导入允许；
* 当前活动入口 `main.py → math_drawing_assistant.bootstrap → math_drawing_assistant.ui.main_window`、AppController 和 package UI 不导入 formal Engine 或 Matplotlib，不存在 GUI 主线程 Engine/Matplotlib 旁路；
* workers 只有锁保护的一个 `pending` 字段，没有 `Queue` / `deque` 无界 FIFO；
* workers 只有 `RenderActor.__init__` 的一个 `QThread()` 构造点，不按请求新建；
* `git diff --check` exit 0。

仓库根目录仍保留一个不在当前活动入口调用图中的旧 V0.1 `main_window.py` / `plot_engine.py`，其中旧窗口导入 `backend_qtagg` 并同步绘制。它不得在阶段 11 被重新接为正式或降级入口；本项“无旁路”结论只针对当前活动 package 入口，遗留文件作为显式剩余风险保留。

### 线程、latest-wins、取消与恢复证据

* `y=x^2` 的 analyzer、spec、viewport、plan、sampler、renderer 六阶段全部记录为同一个唯一 Actor 原生线程，且与 GUI 线程不同；结果通过 queued GUI relay 回到 GUI 线程。批准的 PNG 为 `89 50 4E 47 0D 0A 1A 0A` 签名，包含 `IHDR`，尺寸精确为请求的 641×377。
* 自动 latest-wins 的执行序列精确为 `[201, 203]`：201 已进入正式 sampler 后观察到取消，pending 202 在进入 executor 前被 203 覆盖并取消，只有 203 成功发射。所有真实 probe 的最大同时 Matplotlib 进入数为 1。
* renderer 取消发生在 Figure、Canvas 和 BytesIO 已创建之后；取消结果未发射，资源在 `finally` 释放，下一请求 302 正常返回 PNG。
* `log(x)` 使用项目正式对数语义返回 `LOG_REQUIRES_BASE`，下一请求 `y=x^2` 正常成功。另一个 probe 在正式 sampler 成功后注入普通异常，Actor 将其隔离为 `INTERNAL_ERROR`，下一真实请求仍成功。
* 渲染中 shutdown 取消 current、关闭结果门并在 5,000 ms 批准等待内返回 true；取消结果不发射，Actor 线程停止。独立 shutdown 测试同时覆盖返回 false、`TIMED_OUT` 临时 keepalive 注册、后续重试完成以及无 `QThread: Destroyed while thread is still running`。

### 资源、backend 与 GUI 响应证据

* 连续 20 次真实 formal render 均成功；每次后 `Gcf.get_all_fig_managers()` 为空。最终 20 个 Figure、20 个 Canvas、20 个 BytesIO 均无持久弱引用，20 个 BytesIO 全部 close；60 个 sampled `x` / `y` / `segment_ranges` 数组弱引用全部失效。
* qapp 自动证据在真实 sampler 调用仍被确定性 gate 保持为 active 时，由 GUI `QTimer` 连续处理 3 个事件，三个回调均在 GUI 线程，随后放行并完成正式 PNG；没有用脆弱的严格毫秒阈值。
* fresh process 证据不依赖当前 pytest 进程的模块缓存，确认 Actor/renderer 导入隔离和非交互式直接 Agg 路径。

### 历史非 offscreen Windows 桌面交互观察记录

该历史记录来自 `QT_QPA_PLATFORM=windows` 的真实可见窗口，不是 pytest、offscreen 或 qapp 自动测试。窗口运行 test-only formal executor 与正式 RenderActor；通过 Windows.Graphics.Capture / UI Automation 逐项点击并观察。进程内 `threading.get_ident()` 显示 GUI thread identity 53928、唯一 Actor thread identity 45176，最大 Matplotlib 进入数始终为 1。

| 场景 | 可见操作与记录 | 结果 |
|---|---|---|
| 快速连续提交 / latest-wins | 点击 `Rapid latest-wins`；1001 为 4096×4096 重任务，随后快速提交 1002、1003、1004、1005 | 只出现 1005 success；中间请求无结果；PNG 预览正常；无崩溃 |
| 渲染期间响应 | 重任务显示 `Rendering request 1008`；GUI heartbeat 在可见窗口中从 513/514 继续到 565，按钮和窗口保持可交互 | 1008 success；窗口未冻结 |
| 错误后继续 | 点击 `Formal error → recovery` | 1006 显示 `failure:log_requires_base`，随后 1007 `y=x^2` success；同一 Actor 线程继续 |
| 渲染中关闭 | 1009 明确显示 `Rendering ... 4096×4096` 时点击窗口关闭 | 窗口有序消失，无崩溃；关闭前可见 QThread 运行中析构警告计数为 0 |
| 退出后残留 | 关闭后按窗口标题复查为 0 个窗口；按实际 UI PID 48740、启动 wrapper PID 50764 和 `python*.exe` command line 精确复查 | 无残留 probe 线程对应进程或 Python probe 进程 |

观察期间 GUI heartbeat、历史区和 PNG 预览持续更新；未出现 `QThread: Destroyed while thread is still running`、窗口崩溃或孤儿进程。上述历史记录是当时的真实桌面交互观察，不冒充项目所有者姓名或课堂代表设备验收。

### 历史关闭结论、阻塞发现与剩余风险

首次关闭时登记的三类门禁均成立：

1. 自动测试全部通过；
2. 独立真实 Agg / cancellation / resource / fresh-process probe 全部通过；
3. 非 offscreen Windows 桌面交互观察完成且有上表真实记录。

历史上曾据上述三类门禁登记 P0-04 正式关闭、阶段 10 严格通过。最终只读验收随后发现两个阻塞：真实 Qt worker slot 中逸出的非 `Exception` `BaseException` 会终止 mailbox drain，以及 shutdown timeout 缺少安全上界。最小修复已经实施，历史证据保留但原关闭结论暂时撤回；重开时状态为“等待阶段 10 最终独立复验”，当时未进入阶段 11。后续最终技术复验与重新关闭见第 8 节。

剩余风险：

* 本轮 executor 和可见窗口均为 test-only 证据。阶段 11 仍须把唯一 RenderActor、AppController result relay、正式 `MainWindow.closeEvent` 对 `shutdown=False` 的拒绝退出语义和 `bootstrap` 最终退出门接入产品，并重新执行应用级真实桌面验收；
* 当前 limits 仍是 `INITIAL_SAFETY`，20 次释放和开发机 4096×4096 观察不是课堂代表设备性能、长时 soak 或内存峰值基准；
* 本证据只覆盖当前单项显函数链路，不证明 M1.5 一般直线、圆或圆锥曲线，也不证明 OCR、截图、剪贴板、打包或发布；
* 根目录旧 V0.1 同步 Matplotlib 示例不得成为阶段 11 旁路。

本登记明确**不构成阶段 11 UI 整合结论**。

---

## 8. P0-04 阶段 10 最终技术复验与关闭登记（2026-07-31）

总架构师最终裁定：

```text
阶段 10 技术实现：通过
阶段 10 技术复验：通过
阶段 10 整体裁定：通过
P0-04：技术关闭条件已经满足
```

### Git 基线与复验范围

```text
分支：master
阶段 9 正式基线：d8b023c88ee324f7f8c1cb2e36d24c84e25c8940（feat: add budgeted Agg renderer）
HEAD == origin/master
阶段 10 状态：尚未提交
范围：13 个预期文件（5 个跟踪修改、8 个未跟踪 worker/test 文件）
staged：无
stash：无
git diff --check：通过
阶段 11 正式接线：无
```

13 个范围文件只包含既有阶段 10 文档、AppController、workers 与对应测试；`pyproject.toml`、`uv.lock`、Engine、Models、UI、bootstrap 和活动入口未改变。

### 两个历史阻塞、最小修复与独立复现

1. **真实 Qt worker 的 BaseException 消费边界。** 阶段 10 曾因 `SystemExit` 等非 `Exception` `BaseException` 逸出真实 `_RenderWorker._wake`、毒化消费者并滞留后续 pending 而裁定不通过。修复后，`SystemExit`、`KeyboardInterrupt` 等异常在真实 worker 每任务 drain 边界被遏制，不映射为 `PlotSceneResult`、不发射业务结果或泄露异常详情；current token 在 `finally` / `complete` 中清除，drain 继续消费 pending。独立真实 Actor 探针中 T1 抛 `SystemExit` 后无业务结果，T2 进入同一 worker 并成功，GUI relay 正常，mailbox 最终清空，`shutdown(5000) == True`、lifecycle 为 `STOPPED`、线程停止、registry 为 0；真实 Qt slot 回归也覆盖 `KeyboardInterrupt`。
2. **shutdown timeout 安全域。** 阶段 10 曾因接受可溢出或映射为永久等待的 timeout 而裁定不通过。修复后构造参数和 shutdown override 使用同一 validator，有效范围为 `[0, 60000]` ms；`bool`、非整数、负数和超上限值在任何 lifecycle、mailbox、token、gate、stop、quit、wait 或 registry 变化前拒绝。独立 bomb-spy 探针确认 `60001`、`2**32-1`、`2**64-1` 均被拒绝，`QThread.quit()` 与 `QThread.wait()` 调用数均为 0，非法调用不改变 Actor 状态或取消 current/pending token；释放受控 executor 后 T2、T3 均成功，合法 timeout 的 shutdown 返回 true，最终进入 `STOPPED`、线程停止且 registry 为 0。

### 最终独立测试统计

所有 pytest 命令均使用锁文件，并设置 `PYTHONDONTWRITEBYTECODE=1`、`PYTHONPATH=.`、独立系统临时 `MPLCONFIGDIR` 和 `-p no:cacheprovider`。

| 范围 | 最终独立复验结果 |
|---|---:|
| CancellationToken | 4 passed |
| AppController | 24 passed |
| RenderActor | 23 passed |
| shutdown | 33 passed |
| 正式 Agg 探针 | 9 passed |
| `tests/workers` | 69 passed |
| 阶段 8/9/10 台账组合 | 331 passed |
| 额外加入 `tests/test_errors.py` 的组合 | 344 passed |
| 全量 | 854 passed |

全部运行均为 `failed=0`、`errors=0`、`skipped=0`、`xfail=0`、`xpass=0`、`deselected=0`、`warnings=0`。第 7 节的 840、55、317 等数字仅保留为首次关闭和阻塞修复前的历史统计；本节 854、69、331 为最终独立复验统计。

### 本次非 offscreen Windows 可见探针

本次重新执行 `QT_QPA_PLATFORM=windows` 的真实可见窗口。GUI thread id 为 53564，唯一 worker thread id 为 53516；线程 ID 仅为本次进程证据，不是长期固定值。快速 latest-wins 只发布 request 1005；request 1006 返回 `log_requires_base`，request 1007 随后恢复成功；Matplotlib 最大同时进入数为 1，GUI heartbeat 在渲染期间继续推进。4096×4096 渲染中关闭成功，窗口和 UI PID 46292 消失，残留 probe/pytest 进程为 0，Qt warning 为 0，stderr 为空，隔离临时目录已删除。

### 资源释放与 fresh-process 结果

连续 20 次正式渲染后 `Gcf.get_all_fig_managers()` 为空；20 个 Figure、20 个 Canvas、20 个 BytesIO 均释放，BytesIO 均已关闭，60 个 sampled arrays 弱引用失效。最终 registry 为 0，无本轮残留线程、进程、PNG 或临时目录。

```json
{
  "actor_loaded_matplotlib": false,
  "backend": null,
  "backend_agg_loaded": true,
  "backend_qtagg_loaded": false,
  "interactive": false,
  "pyplot_loaded": false,
  "pyside6_loaded": true
}
```

该 fresh-process 结果说明 RenderActor 模块本身未提前加载 Matplotlib；正式 Agg 路径只加载 `backend_agg`，未加载 `pyplot` 或 `backend_qtagg`；PySide6 因 Actor 的 Qt 边界正常加载。

### 最终复验后的七项非阻塞风险

1. BaseException 后 Controller 可能暂留 `RENDERING`；supersede、cancel、shutdown 均可恢复。
2. `take_pending` 的防御性 `RuntimeError` 位于 drain try 外，但在唯一 affine worker 不变量下不可达。
3. 自动 keepalive 清理依赖 GUI event loop；同步 shutdown 重试仍可清理，且文档禁止 timeout 后直接退出。
4. 根目录旧 `main_window.py`、`plot_engine.py` 不在活动入口调用图，阶段 11 不得重新接回。
5. 私有 lifecycle/mailbox 白盒测试对未来重构较脆弱，但不影响当前产品行为。
6. 历史 840、55、317 必须保留为历史；最终统计为 854、69、331。
7. 当前 Windows 开发机结果不能外推为课堂代表设备结论。

非阻塞测试加固建议：checked-in timeout spy 未来可改为立即失败的 bomb spy，以便顺序回归时更早暴露问题；该建议不构成阶段 10 阻塞项。

### 正式关闭结论与边界

P0-04 现正式关闭。关闭范围只包括 RenderActor、Matplotlib Agg、唯一单消费者、一个 current 加一个 latest-wins pending、协作取消、有界 shutdown 和资源释放。

本关闭明确**不构成阶段 11 UI、bootstrap、MainWindow、preview、clipboard 或 M1 端到端整合验收**。阶段 11 仍须完成 UI → AppController → RenderActor 正式生产接线、result relay、正式 `MainWindow.closeEvent` 对 `shutdown=False` 的拒绝退出语义和 bootstrap 最终退出门，并重新执行应用级桌面验收。

---

## 9. 当前活动状态：阶段 11 关闭登记（2026-08-01）

阶段 11：已通过。P0-04 仍保持关闭；其阶段 10 历史证据与关闭范围不被本登记改写。

正式生产链已存在：package bootstrap 创建唯一 `SceneRenderExecutor` 和唯一 `RenderActor`，手动显函数按 `MainWindow → AppController → RenderActor → SceneRenderExecutor → Engine/Agg → GUI relay → PlotPreview` 流动。`request_id` 和 `scene_revision` 双校验、输入立即 stale、三分 disposition、latest-wins 有界队列、旧成功图保护以及 `shutdown=False` 阻止退出并允许重试，均已通过阶段 11 独立验收。

本登记不扩大阶段范围：ClipboardService 未实现，QClipboard 未接入，目标软件粘贴未验收；正式性能 P50/P95 协议未完成，M1 checkpoint 未完成，阶段 12 尚未开始。
